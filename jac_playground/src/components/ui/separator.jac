cl import from react { * as React }
cl import from "@radix-ui/react-separator" { * as SeparatorPrimitive }
cl import from "...lib.utils" { cn }

cl {
    def: pub Separator(props: dict) -> any {
        return (
            
        );
    }
}
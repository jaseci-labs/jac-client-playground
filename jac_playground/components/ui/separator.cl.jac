import from react { * as React }
import from "@radix-ui/react-separator" { * as SeparatorPrimitive }
import from "...lib.utils" { cn }

def: pub Separator(props: dict) -> JsxElement {
    return (
        
    );
}